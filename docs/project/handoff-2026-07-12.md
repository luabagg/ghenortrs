# Handoff — site search and polish

Status em 2026-07-12. Implementação ainda não finalizada, commitada ou integrada.

## Implementado

- Busca desktop e mobile com índice gerado do sitemap Nuvemshop.
- Índice remove URLs `/pt/` duplicadas, valida XML e mantém fallback oficial `/search/?q=`.
- Busca tolera acentos, ranqueia termos e suporta teclado.
- Flicker do menu corrigido; medição desktop passou de 22 px para 0 px.
- Navegação `Tecnologia` removida; logo continua levando à home.
- Inventário corrigido: pastilhas, cubos e aros compráveis; rotores e mass dampers via contato.
- Tipografia, textos, cards, destaques operacionais e CTAs refinados.
- Zoom/parallax no scroll, com suporte a `prefers-reduced-motion`.
- SEO/GEO ampliados: metadados, alt text, JSON-LD, sitemap, skip link e `llms.txt`.
- Mapa de destinos comerciais e especificação da busca documentados.

## Falta

- Atualizar testes antigos em `src/app.test.tsx` para novo texto e destinos.
- Sincronizar `current-focus.md`, `milestones.md`, `timeline.md`, M7, M8 e `checkout-boundary.md`.
- Remover afirmações antigas sobre `liquid-glass-react`, busca bloqueada, login B2B e inventário.
- Rodar gates finais: testes, lint, typecheck, format, build e `git diff --check`.
- Fazer QA desktop 1440×900 e mobile 390×844, incluindo busca, rotas, overflow, motion e console.
- Inspecionar arquivos finais em `dist/`.
- Atualizar Linear `LUA-49`, `LUA-53` e M8. Bloqueado: ambiente não possui conector Linear gravável.
- Só depois dos gates: escolher integração, commit/PR ou preservação do branch.

## Worktrees

### Trabalho ativo

```bash
cd /home/luanb/development/ghenortrs/.worktrees/site-search-polish
rtk git status --short
```

- Branch: `feat/site-search-polish`
- Base: `b76e9d7`
- Continuar implementação e verificação aqui.

### Worktree principal

```bash
cd /home/luanb/development/ghenortrs
rtk git status --short
```

Contém mudanças do usuário em `AGENTS.md`, `CLAUDE.md`, `package.json` e `pnpm-lock.yaml`. Não restaurar, sobrescrever ou misturar com este trabalho.

### Worktrees históricos

```text
.worktrees/bugs         improve/bugs-audit
.worktrees/bugs-final   improve/bugs-final
.worktrees/design       improve/modern-design
.worktrees/seo-geo      improve/seo-geo
.worktrees/typography   improve/typography-copy
```

Não editar ou apagar. Antes de qualquer integração, confirmar estado com:

```bash
cd /home/luanb/development/ghenortrs
rtk git worktree list
```

Não fazer merge, rebase, commit, push ou remoção de worktree sem decisão explícita do usuário e gates finais aprovados.
